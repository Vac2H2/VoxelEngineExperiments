# ShapeDataContainerFactory Jobs

This folder is reserved for future factory-side jobs.

Potential uses:

- Copy input chunk bitplanes into authoritative chunk storage.
- Build `Chunks.IndexByPosition` from input positions.
- Validate or compact input chunk data before container creation.

No jobs are defined yet. The current factory implementation performs initialization synchronously.
